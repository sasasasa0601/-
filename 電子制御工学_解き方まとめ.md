# 電子制御工学（Lect.08〜12）問題パターン別・解き方まとめ

「この聞かれ方が来たらこの手順で解く」形式のまとめ。
行列式・固有値・逆行列・対角化の計算はできる前提で、**手順の選び方と流れ**だけに絞ってある。

---

## 0. まず問題文を見て解法を選ぶ（早見表)

| 問題文の聞かれ方 | 使う解法 | 使う行列・式 |
|---|---|---|
| 「次のシステムの安定性を調べなさい」（ẋ=Ax のみ、b・c なし） | フルビッツの安定判別 | 特性方程式 \|sI−A\|=0 とフルビッツ行列 H |
| 「安定性を調べなさい」（b・c 付き／伝達関数・有界入出力の文脈） | 漸近安定＋有界入出力安定の両方を判定 | \|sI−A\|=0 と G(s)=c(sI−A)⁻¹b |
| 「リヤプノフの方法で安定性を調べなさい」 | リヤプノフ方程式 | AᵀP+PA=−Q（Q=I とおく） |
| 「設計パラメータ h=◯ の直接フィードバック…閉ループ系の極を求め、安定化できたか」 | 直接フィードバック | 閉ループ系行列 (A−hbc) の固有値 |
| 「極を λ₁, λ₂ に配置する状態フィードバック制御則を求めなさい」 | 極配置（可制御正準形経由） | f = f̄T⁻¹、T = M_c·S |
| 「λ₁, λ₂ を極にもつオブザーバを構成しなさい」 | 双対性を使った極配置 | (Aᵀ, cᵀ) に極配置 → k = fᵀ |
| 「併合システムを構成し、その極を求めなさい」 | 状態FB＋オブザーバの合成 | ブロック行列 → 極は両者の極の和集合 |
| 「最小分散制御則を求めなさい」（y(t)=z⁻ᵏ B/A u(t) の形） | 最小分散制御 | Diophantine方程式 1 = AE + z⁻ᵏF |
| 「一般化最小分散制御則を求めなさい」（P, Q, R が与えられる） | 一般化最小分散制御 | P = AE + z⁻ᵏᵐF |

判定のコツ：
- **行列 A だけ**が与えられて「安定性」→ フルビッツ
- **b, c も**与えられて「安定性」→ 漸近安定と有界入出力安定は**別物**として両方調べる
- 「リヤプノフ」と明示されたら固有値を求めずに P を解く
- **z⁻¹ が出てきたら**（離散時間・むだ時間 k）→ 最小分散系の問題

---

## 1. フルビッツの安定判別 —「安定性を調べなさい」（A のみ）

**手順**
1. 特性方程式を求める：|sI−A| = sⁿ + a_{n−1}sⁿ⁻¹ + … + a₁s + a₀ = 0
2. **条件1**：係数 aᵢ が**すべて存在し（欠けがなく）、すべて正**であることを確認
3. **条件2**：フルビッツ行列 H を作る
   - 1行目に a_{n−1}, a_{n−3}, a_{n−5}, …、2行目に 1, a_{n−2}, a_{n−4}, … と並べ、行が下がるごとに右へ1つずらす
4. 首座小行列式 h₁, h₂, … を計算し、**すべて正**なら**漸近安定**

**3次の場合の型**（s³ + a₂s² + a₁s + a₀ = 0）：

```
      | a₂  a₀  0 |
  H = | 1   a₁  0 |        h₁ = a₂ > 0
      | 0   a₂  a₀|        h₂ = a₂a₁ − a₀ > 0   （h₃ = a₀·h₂ なので a₀>0 なら自動的にOK）
```

**ノートの例題**：s³ + 7s² + 13s + 4 = 0 → h₁ = 7 > 0、h₂ = 7·13 − 4·1 = 87 > 0、係数もすべて正 → **漸近安定である**。

**結論の書き方**：「特性方程式の係数がすべて存在しすべて正であり、フルビッツの小行列式がすべて正であるため、このシステムは漸近安定である。」

---

## 2. 有界入出力安定 —「安定性を調べなさい」（A, b, c 付き）

漸近安定と有界入出力（BIBO）安定を**別々に**判定するのがポイント。

**手順**
1. **漸近安定の判定**：|sI−A| = 0 の根（固有値）を求める。実部がすべて負なら漸近安定。**実部が正の根が1つでもあれば漸近安定でない。**
2. **有界入出力安定の判定**：伝達関数を計算する
   - G(s) = c(sI−A)⁻¹b
   - 2次なら (sI−A)⁻¹ = (1/|sI−A|)·(余因子行列) で機械的に計算
   - **分子と分母を約分する**（ここが最重要。共通因子が消える）
3. 約分後に残った**極の実部がすべて負**なら有界入出力安定

**理屈（記述問題で聞かれる）**
- 対角正準形にしたとき、b̄ の成分 β や c̄ の成分 γ に **0 があれば可制御／可観測でない** → その固有値は伝達関数の極に**現れない**（約分で消える）
- 可制御かつ可観測なら「特性方程式の根＝伝達関数の極」なので**漸近安定 ⇔ 有界入出力安定**
- 可制御・可観測でない場合、**漸近安定でなくても有界入出力安定になり得る**

**ノートの例題**：A = [[2,2],[2,−1]], b = [−1,2]ᵀ, c = [−1,1]
- |sI−A| = (s−3)(s+2) = 0 → 極 3 の実部が正 → **漸近安定でない**
- G(s) = (3s−9)/((s−3)(s+2)) = **3/(s+2)** ← (s−3) が約分で消える
- 残った極 −2 は実部負 → **有界入出力安定である**
- 結論：「漸近安定でないが、有界入出力安定である。」

---

## 3. リヤプノフの安定判別 —「リヤプノフの方法で調べなさい」

固有値を求めずに安定判別する方法。**P を未知数として連立方程式を解く**問題。

**手順**
1. Q = I（単位行列）とおき、リヤプノフ方程式を立てる：
   **AᵀP + PA = −I**
2. P は対称行列とおく（2次なら P = [[p₁, p₂], [p₂, p₃]] の**3未知数**）
3. 左辺を計算して成分比較 → p₁, p₂, p₃ の連立1次方程式を解く
4. 求めた P が**正定行列**かを首座小行列式で確認：
   - p₁ > 0 かつ |P| = p₁p₃ − p₂² > 0
5. P が正定なら**漸近安定**

**ノートの例題**：A = [[0,1],[−2,−2]] のとき
- 成分比較 → −4p₂ = −1, p₁ − 2p₂ − 2p₃ = 0, 2p₂ − 4p₃ = −1
- → p₂ = 1/4, p₃ = 3/8, p₁ = 5/4
- 判定：p₁ = 5/4 > 0、|P| = (5/4)(3/8) − (1/4)² = 13/32 > 0 → P は正定 → **漸近安定**

**知識問題用**：V(x) > 0 (x≠0), V(0) = 0, V̇(x) < 0 ならば漸近安定。この V(x) = xᵀPx をリヤプノフ関数という。正定行列＝「すべての首座小行列式が正の対称行列」。

---

## 4. 直接フィードバック —「h=◯ としたときの閉ループ系の極を求め、安定化できたか」

**手順**
1. まず**元のシステム** A の固有値を求める（|sI−A| = 0）→ 実部正の固有値があれば「不安定」と述べる
2. 閉ループ系のシステム行列を作る：**A − hbc**（h はスカラー、bc は列×行で行列になる）
3. |sI − (A−hbc)| = 0 を解いて閉ループ系の極を求める
4. 実部がすべて負になっていれば「安定化できた」、なっていなければ「安定化できなかった」

**ノートの例題**：A = [[−1,0],[2,1]], b = [0,1]ᵀ, c = [1,1], h = 4
- 元の固有値：−1 と 1 → 不安定
- A − 4bc = [[−1,0],[2,1]] − 4[[0,0],[1,1]] = [[−1,0],[−2,−3]]
- 閉ループ極：−1 と −3 → すべて実部負 → **安定化できた**

**知識問題用**：制御則は u(t) = v(t) − h**y**(t)（出力を戻す）。設計パラメータは **1個**しかないので、n 個の極を自在に配置できず、**安定化できるとは限らない**。

---

## 5. 極配置（状態フィードバック設計）—「極を λ₁, λ₂ に配置する制御則を求めなさい」

**この5ステップを暗記する（毎回同じ流れ）**

1. **希望の特性多項式**：(s−λ₁)(s−λ₂) = s² + d₁s + d₀ → d₁, d₀ を控える
2. **可制御性の確認**：M_c = [b Ab]、|M_c| ≠ 0 → 「可制御である」と一言書く
3. **元のシステムの特性方程式**：|sI−A| = s² + a₁s + a₀ → a₁, a₀ を控える
4. **変換行列 T** を作る：**T = M_c · S**、ここで（2次のとき）

   ```
   S = | a₁  1 |
       | 1   0 |
   ```
   （n次なら S の1行目は a₁, a₂, …, a_{n−1}, 1 で、下の行ほど左に詰まる）
5. **フィードバック係数ベクトル**：
   **f = [d₀−a₀  d₁−a₁] T⁻¹**
   制御則は **u(t) = −f x(t)**（v(t)付きなら u = v − fx）

**ノートの例題**：A = [[−1,−2],[−1,−4]], b = [1,1]ᵀ、希望極 −1, −2
1. (s+1)(s+2) = s² + 3s + 2 → d₁=3, d₀=2
2. M_c = [[1,−3],[1,−5]]、|M_c| = −2 ≠ 0 → 可制御
3. |sI−A| = (s+1)(s+4) − 2 = s² + 5s + 2 → a₁=5, a₀=2
4. T = [[1,−3],[1,−5]]·[[5,1],[1,0]] = [[2,1],[0,1]]
5. f = [2−2  3−5] T⁻¹ = [0 −2]·[[1/2,−1/2],[0,1]] = **[0 −2]** → u(t) = −[0 −2]x(t)

**検算のポイント**：|sI−(A−bf)| を展開すると①で立てた s²+d₁s+d₀ に一致するはず。

---

## 6. オブザーバの設計 —「λ₁, λ₂ を極にもつオブザーバを構成しなさい」

**考え方：双対性**。オブザーバの設計＝**転置した系 (Aᵀ, cᵀ) に対する極配置**。§5の手順がそのまま使い回せる。

**手順**
1. **可観測性の確認**：M_o = [c; cA]（cを上、cAを下に縦に並べる）、|M_o| ≠ 0 → 「可観測である」
2. 希望のオブザーバ極から特性多項式：(s−λ₁)(s−λ₂) = s² + d₁s + d₀
3. **A → Aᵀ、b → cᵀ に置き換えて §5 の極配置を実行**：
   - |sI−Aᵀ| = s² + a₁s + a₀（|sI−A| と同じになる）
   - T = [cᵀ  Aᵀcᵀ]·S
   - f = [d₀−a₀  d₁−a₁] T⁻¹
4. **k = fᵀ**（転置して縦ベクトルに戻す）
5. オブザーバを書き下す：
   **x̂̇(t) = (A−kc) x̂(t) + b u(t) + k y(t)**
   （A−kc を数値で計算して代入した形が最終解答）

**ノートの例題**：A = [[−1,−2],[−1,−4]], c = [0,1], b = [1,1]ᵀ、オブザーバ極 −4, −5
1. M_o = [[0,1],[−1,−4]]、|M_o| = 1 ≠ 0 → 可観測
2. (s+4)(s+5) = s² + 9s + 20 → d₁=9, d₀=20
3. |sI−Aᵀ| = s² + 5s + 2、T = [[0,−1],[1,−4]]·[[5,1],[1,0]] = [[−1,0],[1,1]]
   f = [20−2  9−5] T⁻¹ = [18 4]·[[−1,0],[1,1]] = [−14 4]
4. **k = [−14, 4]ᵀ**
5. A − kc = [[−1, −2−(−14)],[−1, −4−4]] = [[−1,12],[−1,−8]] より
   x̂̇ = [[−1,12],[−1,−8]] x̂ + [1,1]ᵀ u + [−14,4]ᵀ y

**知識問題用**：オブザーバ＝状態変数を推定する仕組み（推定器）。誤差 e(t) = x̂−x は ė = (A−kc)e に従うので、**(A−kc) の固有値の実部をすべて負**にすれば e→0 となり推定できる。条件は**可観測**であること。

---

## 7. 併合システム —「併合システムを構成し、その極を求めなさい」

状態フィードバック（§5）とオブザーバ（§6）を**両方設計してから合体**させる問題。

**手順**
1. §5 の手順で f を求める（条件：可制御）
2. §6 の手順で k を求める（条件：可観測）
3. 状態変数を (x, e)（e = x̂ − x）に取り替えると、併合システムは**ブロック上三角**になる：

   ```
   [ẋ]   [ A−bf   −bf  ] [x]
   [ė] = [  0    A−kc ] [e]
   ```
4. ブロック上三角なので固有値は対角ブロックのものを合わせたもの：
   **|sI−(A−bf)| · |sI−(A−kc)| = 0**
5. よって**併合システムの極 ＝ 状態フィードバックの極 ∪ オブザーバの極**（設計した値がそのまま答え）

**ノートの例題**：§5・§6 の続き（f = [0 −2], k = [−14,4]ᵀ）→ 併合システムの極は **−1, −2, −4, −5**。

**知識問題用**：
- 状態FBで安定化する条件：**可制御** ／ オブザーバで推定する条件：**可観測**
- 可制御かつ可観測なら、レギュレータの極とオブザーバの極を**それぞれ独立に任意配置できる**（分離定理）
- 制御則は u(t) = v(t) − f **x̂**(t)（実際の x でなく推定値を使う）

---

## 8. 最小分散制御 —「最小分散制御則を求めなさい」（z⁻¹ の式が出たら）

与えられるもの：A(z⁻¹), B(z⁻¹), むだ時間 k。

**手順**
1. **E, F の次数を決める**：
   - E(z⁻¹) の次数 = **k − 1**（E = 1 + e₁z⁻¹ + … + e_{k−1}z^{−(k−1)}、定数項は必ず1）
   - F(z⁻¹) の次数 = **n − 1**（n は A の次数。F = f₀ + f₁z⁻¹ + …）
2. **Diophantine方程式**を立てる：
   **1 = A(z⁻¹)E(z⁻¹) + z⁻ᵏ F(z⁻¹)**
3. 左辺を展開し、z⁻¹ の**各次数の係数比較**で e₁, …, f₀, … を求める
   （右辺は 1 なので、z⁻¹ 以上の係数はすべて 0 になる）
4. 制御則に代入：
   **u(t) = 1 / (E(z⁻¹)B(z⁻¹)) · { w(t) − F(z⁻¹) y(t) }**
5. **時間領域の式に直す**：E·B を展開して両辺に掛け、z⁻¹u(t) = u(t−1) 等に戻し、u(t) について解く

**ノートの例題**：A = 1 − 0.6z⁻¹, B = 0.2 + 0.1z⁻¹, k = 2
1. E の次数 = 1、F の次数 = 0
2. 1 = (1−0.6z⁻¹)(1+e₁z⁻¹) + z⁻²f₀
3. z⁻¹：e₁ − 0.6 = 0 → **e₁ = 0.6**、z⁻²：f₀ − 0.6e₁ = 0 → **f₀ = 0.36**
4. u(t) = 1/{(1+0.6z⁻¹)(0.2+0.1z⁻¹)}·{w(t) − 0.36y(t)}
5. (0.2 + 0.22z⁻¹ + 0.06z⁻²)u(t) = w(t) − 0.36y(t) より
   **u(t) = (1/0.2){w(t) − 0.36y(t) − 0.22u(t−1) − 0.06u(t−2)}**

**知識問題用**：評価規範 J = E[{y(t+k) − w(t)}²]。y(t+k) は時刻 t では取得できないため、Diophantine方程式で**未来の出力を過去～現在の入出力で予測**してから J を最小化する。A, B のパラメータを逐次最小二乗法で推定しながら制御則も更新するのがセルフチューニングコントロール。

---

## 9. 一般化最小分散制御 —「一般化最小分散制御則を求めなさい」（P, Q, R 付き）

§8 との違いは2か所だけ：**Diophantine方程式の左辺が P になる**ことと、**分母に Q が足される**こと。

**手順**
1. E の次数 = k_m − 1、F の次数 = n − 1（§8 と同じ）
2. Diophantine方程式：
   **P(z⁻¹) = A(z⁻¹)E(z⁻¹) + z^{−k_m} F(z⁻¹)**
3. 係数比較で E, F を決める（右辺の各係数を **P の係数に**合わせる。0 ではないことに注意）
4. 制御則：
   **u(t) = 1 / (E·B + Q) · { R(z⁻¹)w(t) − F(z⁻¹)y(t) }**
5. 展開して時間領域の漸化式に直す

**ノートの例題**：A = 1−0.6z⁻¹, B = 0.4+0.1z⁻¹, k_m = 2, P = 1−0.9z⁻¹+0.2z⁻², Q = 1, R = 0.6+0.4z⁻¹
2. 1−0.9z⁻¹+0.2z⁻² = (1−0.6z⁻¹)(1+e₁z⁻¹) + z⁻²f₀
3. z⁻¹：e₁−0.6 = **−0.9** → e₁ = −0.3、z⁻²：f₀−0.6e₁ = **0.2** → f₀ = 0.02
4. 分母：(1−0.3z⁻¹)(0.4+0.1z⁻¹) + 1 = 1.4 − 0.02z⁻¹ − 0.03z⁻²
5. **u(t) = (1/1.4){0.6w(t) + 0.4w(t−1) + 0.02u(t−1) + 0.03u(t−2) − 0.02y(t)}**

**知識問題用**：最小分散制御は瞬時に目標値へ一致させようとするため**入力が過大になり応答が振動的**になりやすい。評価規範に入力の大きさ（Q項）を加えて抑えるのが一般化最小分散制御。

---

## 付録：これだけは覚える公式・行列

| 名前 | 式 | 使う場面 |
|---|---|---|
| 可制御性行列 | M_c = [b  Ab  …  Aⁿ⁻¹b]、\|M_c\|≠0 で可制御 | 極配置の前チェック |
| 可観測性行列 | M_o = [c; cA; …; cAⁿ⁻¹]、\|M_o\|≠0 で可観測 | オブザーバの前チェック |
| 直接FB閉ループ | A − hbc | h指定の安定化問題 |
| 状態FB閉ループ | A − bf | 極配置の検算 |
| オブザーバ誤差系 | A − kc | オブザーバの極 |
| 変換行列 | T = M_c·S（S は a₁,…,1 を斜めに並べた行列） | 極配置ステップ4 |
| FB係数 | f = [d₀−a₀ … d_{n−1}−a_{n−1}] T⁻¹ | 極配置ステップ5 |
| 双対性 | A→Aᵀ, b→cᵀ, c→bᵀ, k = fᵀ | オブザーバ設計 |
| リヤプノフ方程式 | AᵀP + PA = −Q | リヤプノフ判別 |
| 伝達関数 | G(s) = c(sI−A)⁻¹b（必ず約分） | BIBO安定 |
| Diophantine | 1 = AE + z⁻ᵏF（一般化は P = AE + z^{−k_m}F） | 最小分散系 |
| 最小分散制御則 | u = (w − Fy)/(EB)（一般化は u = (Rw − Fy)/(EB+Q)） | 制御則の最終形 |

### 用語の対応（記述対策）
- **漸近安定**：A の固有値（特性方程式の根）の実部がすべて負
- **有界入出力安定**：伝達関数 G(s) の（約分後の）極の実部がすべて負
- **可制御・可観測 ⇔** 特性方程式の根 = 伝達関数の極 **⇔** 漸近安定 = 有界入出力安定
- **レギュレータ**：状態を平衡状態に戻す制御系。その極 = (A−bf) の固有値
- **オブザーバ**：状態変数の推定器。その極 = (A−kc) の固有値
- **適応制御**：不確かさを時々刻々推定して設計を更新（モデル規範型・セルフチューニング）／**ロバスト制御**：不確かさを予測した範囲内で安定に設計（H∞制御）
